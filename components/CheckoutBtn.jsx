"use client";
import { useShoppingCart } from "use-shopping-cart";

const CheckoutBtn = () => {
  return (
    <button className="btn btn-primary w-full mb-6">Proceed to CheckOut</button>
  );
};

export default CheckoutBtn;
